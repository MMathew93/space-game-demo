class Hero
  #class will help us ID the hero and allow
  #us to assign special moves later on for that specific hero
  def initialize(hero_class, is_active, name, health, attack_power, defense, sprite)
    @hero_class = hero_class
    @is_active = is_active
    @name = name
    @health = health
    @attack_power = attack_power
    @defense = defense
    @sprite = sprite
  end

  #render the hero

  #attack method

  #defend method

  #if active hero, mess with menu wheel

  #if active, able to deactive and set a different hero as active
  


end